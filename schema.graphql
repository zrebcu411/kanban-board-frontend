# source: http://localhost:4000/graphql
# timestamp: Sat Dec 22 2018 16:08:05 GMT+0100 (GMT+01:00)

type Mutation {
  signin(email: String!, password: String!): SignInResponse!
  signup(email: String!, name: String!, password: String!): SignUpResponse!
}

type Query {
  users: [User!]
}

enum SignInError {
  PASSWORD_INVALID
  USER_DOES_NOT_EXIST
}

type SignInResponse {
  errors: [SignInError!]!
  result: SignUpResult
}

enum SignUpError {
  EMAIL_ALREADY_USED
}

type SignUpResponse {
  errors: [SignUpError!]!
  result: SignUpResult
}

type SignUpResult {
  token: String!
}

type User {
  email: String!
  id: ID!
  name: String!
  passwordHash: String!
}
